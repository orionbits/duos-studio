"use server";

// Project specific server actions can be placed here.
// Currently the studio operates primarily with client-side logic and persistence.

export async function placeholderAction() {
  return { ok: true };
}
